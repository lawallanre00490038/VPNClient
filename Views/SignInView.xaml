<Window x:Class="VpnClient.Views.SignInView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VpnClient.Views"
        xmlns:viewmodels="clr-namespace:VpnClient.ViewModels"
        mc:Ignorable="d"
        Title="Sign In" Height="350" Width="400"
        x:Name="SignInWindow">
    <Window.DataContext>
        <viewmodels:SignInViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Username:" Margin="0,0,0,5"/>
            <TextBox Width="200" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBlock Text="Password:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Width="200" Margin="0,0,0,10" PasswordChanged="PasswordBox_PasswordChanged"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,10"/>
            <Button Content="Sign In" Command="{Binding SignInCommand}" CommandParameter="{Binding ElementName=SignInWindow}" Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</Window>

