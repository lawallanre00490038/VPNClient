<Window x:Class="VpnClient.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:VpnClient.ViewModels"
        mc:Ignorable="d"
        Title="VpnClient" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Connection Status: " FontWeight="Bold"/>
            <TextBlock Text="{Binding ConnectionStatus}"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding VpnNodes}" SelectedItem="{Binding SelectedVpnNode}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="Country" DisplayMemberBinding="{Binding Country}" Width="100"/>
                    <GridViewColumn Header="Latency (ms)" DisplayMemberBinding="{Binding Latency_ms}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Connect" Command="{Binding ConnectCommand}" Margin="0,0,10,0" Width="100" Height="30"/>
            <Button Content="Disconnect" Command="{Binding DisconnectCommand}" Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</Window>

